const enemies = [];

const max_countdown = 1000;

let countdown = max_countdown;

const tick_enemies = (deltatime) => {
  for (const enemy of enemies) {
    
  }
};

export const enemies = (deltatime) => {
  
};
